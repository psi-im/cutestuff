test:
  httppoll
  sasl
  xmlenc

code:
  bytestream: document or remove write()'s return value
  cipher: generate random keys (right now it is all static)
  httppoll: use random keys
  xhtml
  qssl: server support, certificate information, hostname verify?
  securestream: wrap QSSLFilter as ByteStream
  idle
  import misha's iconset code

port:
  win32: bconsole
  mac: globalaccel (eric)

document:
  sha1
  servsock
  srvresolver
  bsocket

