code:
  securestream
  httpconnect
    auth
  bconsole
    writing / closing
  adconn
    if necessary, wait for delayedCloseFinished() before exiting
  merge utils: base64, sha1
  idle

document:
  iconset
  idle

